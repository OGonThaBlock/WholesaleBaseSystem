@model WholesaleBase.ViewModels.DeliveryWithContentsViewModel

@{
    ViewData["Title"] = "Create Delivery with Contents";
}

<h1>Create Delivery with Contents</h1>
<div>
    <a asp-action="ViewDeliveriesWithContents">Back to List</a>
</div>
<form asp-action="CreateWithContents" method="post">
    <div class="form-group">
        <label asp-for="Supplier_id" class="control-label"></label>
        <select asp-for="Supplier_id" class="form-control" asp-items='new SelectList(ViewBag.Suppliers, "Supplier_id", "Name")'></select>
        <span asp-validation-for="Supplier_id" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="CurrentStatus" class="control-label"></label>
        <input asp-for="CurrentStatus" class="form-control" value="Ожидает" readonly />
        <span asp-validation-for="CurrentStatus" class="text-danger"></span>
    </div>

    <h4>Delivery Contents</h4>
    <table class="table">
        <thead>
            <tr>
                <th>Product</th>
                <th>Amount</th>
                <th></th>
            </tr>
        </thead>
        <tbody id="deliveryContentsTable">
            @for (int i = 0; i < Model.DeliveryContents.Count; i++)
            {
                <tr>
                    <td>
                        <select asp-for="@Model.DeliveryContents[i].Product_id" class="form-control"
                                asp-items='new SelectList(ViewBag.Products, "Product_id", "Name")'>
                            <option value="">-- Select Product --</option>
                        </select>
                    </td>
                    <td>
                        <input asp-for="@Model.DeliveryContents[i].Amount" class="form-control" />
                    </td>
                    <td>
                        <button type="button" class="btn btn-danger" onclick="removeRow(this)">Remove</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <button type="button" class="btn btn-success" onclick="addRow()">Add Product</button>

    <div class="form-group mt-3">
        <input type="submit" value="Create" class="btn btn-primary" />
    </div>
</form>

@section Scripts {
    <script>
        function addRow() {
            const table = document.getElementById('deliveryContentsTable');
            const rowCount = table.rows.length;
            const row = table.insertRow(rowCount);

            row.innerHTML = `
                <td>
                    <select name="DeliveryContents[${rowCount}].Product_id" class="form-control">
                        <option value="">-- Select Product --</option>
        @foreach (var product in ViewBag.Products)
        {
                                <option value="@product.Product_id">@product.Name</option>
        }
                    </select>
                </td>
                <td>
                    <input name="DeliveryContents[${rowCount}].Amount" class="form-control" />
                </td>
                <td>
                    <button type="button" class="btn btn-danger" onclick="removeRow(this)">Remove</button>
                </td>
            `;
        }
    </script>
}