@model WholesaleBase.Models.Delivery

@{
    ViewData["Title"] = "Change Status";
}

<h1>Change Delivery Status</h1>

<h4>Delivery</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="ChangeStatus">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Number" />

            <div class="form-group">
                <label asp-for="CurrentStatus" class="control-label"></label>
                <select asp-for="CurrentStatus" class="form-control" asp-items="ViewBag.StatusList"></select>
                <span asp-validation-for="CurrentStatus" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Change Status" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="ViewDeliveriesWithContents">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}